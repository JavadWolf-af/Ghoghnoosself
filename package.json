{
  "name": "ghoghnoosself-bot",
  "version": "1.1.0",
  "description": "Telegram self-service bot with wallet, referral, profile, and token system",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "start": "NODE_ENV=production node --enable-source-maps dist/index.mjs",
    "dev": "node build.mjs && node --enable-source-maps dist/index.mjs"
  },
  "dependencies": {
    "@types/node-telegram-bot-api": "^0.64.15",
    "node-telegram-bot-api": "^1.1.1",
    "pino": "^9.14.0",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "0.25.8",
    "esbuild-plugin-pino": "^2.3.3",
    "typescript": "^5.9.0"
  }
}
